{
  "name": "pf-design-system-generator",
  "version": "1.0.0",
  "description": "Figma Plugin - 디자인 시스템 자동 생성",
  "scripts": {
    "build": "esbuild code.ts --bundle --outfile=dist/code.js --format=iife --target=es2017",
    "watch": "esbuild code.ts --bundle --outfile=dist/code.js --format=iife --target=es2017 --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
